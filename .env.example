# SubHub 环境变量示例 —— 复制为 .env 并按需修改(可选)
# 不配置也能跑,默认值见 README「常用环境变量」

# API 令牌(不填则自动生成并写入 data/api-token)
# API_TOKEN=

# 请求超时(秒)
# REQUEST_TIMEOUT=25

# 搜索缓存时长(秒)
# SEARCH_CACHE_TTL=600

# 站点地址(一般无需修改)
# SUBHD_BASE=https://subhd.tv
# ZIMUKU_BASE=https://zimuku.org
# ASSRT_API_BASE=https://api.assrt.net
# OPENSUBTITLES_API_BASE=https://api.opensubtitles.com
